#!/usr/bin/expect

set CTRLX \030

spawn telnet -8 [lrange $argv 0 0]

interact {
    :wq { send $CTRLX; send "\n" } # :wq replace Ctrl+x enter
}

interact timeout 100 { send "\x00" } # send "\x00" every 10 seconds

exit
